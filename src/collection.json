{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "presenter": {
      "aliases": ["p"],
      "description": "generate a clean presenter module",
      "factory": "./clean/modules/presenter#presenter"
    },
    "domain": {
      "aliases": ["d"],
      "description": "generate a clean domain module",
      "factory": "./clean/modules/domain#domain"
    },
    "data": {
      "aliases": ["r"],
      "description": "generate a clean data module",
      "factory": "./clean/modules/data#data"
    },
    "all": {
      "aliases": ["a"],
      "description": "generate all clean modules",
      "factory": "./clean/all"
    }
  }
}
